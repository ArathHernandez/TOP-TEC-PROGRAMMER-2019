#include <iostream>
#include <math.h>
#include <iomanip>
#include <string>
#include <algorithm>

using namespace std;

int main()
{
    int tamA, tamB;
    string a, b, conteo;
    int sumador = 0, contador = 0;;
    int auxA, auxB;
    cin >> tamA;
    cin >> tamB;
    cin >> a;
    cin >> b;

    for(int i = 0; i < tamB; i++)
    {
        if(b[i] == '1' )
        {
            contador++;
        }

        conteo[i] = contador;
    }

    while(tamB > 0)
    {
    }

    cout << sumador << endl;

    return 0;
}
